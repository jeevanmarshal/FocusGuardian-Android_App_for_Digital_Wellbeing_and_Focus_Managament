Perfect — thank you for explaining why the agent failed.
You are absolutely right: the previous prompts were too compact and abstract for Anti-Gravity Gemini-3 (Pro), so it “filled gaps” on its own and produced abnormal / incorrect design & wiring.
Below is a RESET + REBUILD instruction set, written in a way that:
•	A machine agent can’t misinterpret
•	Each phase explains WHAT to understand first
•	Then WHAT to keep
•	Then WHAT to delete
•	Then WHAT to redesign
•	Then WHAT NOT to touch
•	And WHAT the output of the phase must look like
This is the level an autonomous agent needs.
________________________________________
🔁 RESET INSTRUCTION (VERY IMPORTANT – GIVE THIS FIRST)
🔴 MASTER RESET PROMPT (SEND ALONE)
You are redesigning an Android project called “Focus Guardian”.

IMPORTANT RULES:
1. DO NOT invent new features.
2. DO NOT simplify logic.
3. DO NOT auto-design UI unless explicitly instructed.
4. FOLLOW THE DOCUMENT EXACTLY (“Core Implementation.docx”).
5. WORK PHASE BY PHASE ONLY.
6. DO NOT MOVE TO NEXT PHASE unless current phase goal is satisfied.
7. IF A FILE DOES NOT MATCH DOCUMENT RESPONSIBILITY → DELETE IT.
8. IF A FILE PARTIALLY MATCHES → REFACTOR, NOT DUPLICATE.

Your job is NOT to make it work “somehow”.
Your job is to make the project MATCH THE DOCUMENT EXACTLY.
A visually similar UI is considered FAILURE if logic/flow is wrong.
________________________________________
🧱 PHASE 1 — DOCUMENT UNDERSTANDING (NO CODE, NO UI)
🎯 PURPOSE
Prevent hallucination. The agent must first understand the document, not the code.
📌 PHASE 1 PROMPT
PHASE 1 — DOCUMENT ANALYSIS ONLY

1. Read the entire “Core Implementation.docx”.
2. Extract and list:
   a) All features (apps, sites, shorts, reels, modes, analytics, overlays)
   b) All user flows (what user clicks → what happens)
   c) All enforcement stages (gentle, reminder, strict, enforcement)
   d) All UI screens explicitly mentioned
3. Write a STRUCTURED INTERNAL MAP:
   - Feature → Required Screens → Required Logic → Required Storage

RULES:
- DO NOT look at existing code.
- DO NOT design UI.
- DO NOT write implementation.
- OUTPUT IS ONLY UNDERSTANDING.
✅ Phase success when:
The agent can describe the app without referencing code at all.
________________________________________
🧹 PHASE 2 — EXISTING PROJECT AUDIT (READ-ONLY)
🎯 PURPOSE
Stop blind reuse. Decide what is valid and what is garbage.
📌 PHASE 2 PROMPT
PHASE 2 — PROJECT AUDIT (NO MODIFICATION)

1. Scan the entire current project.
2. For EACH file, classify it as:
   A) Matches document responsibility → KEEP
   B) Partially matches → REFACTOR LATER
   C) Wrong responsibility → DELETE
3. Specifically detect and list:
   - UI classes containing logic
   - Services deciding enforcement
   - Duplicate blockers
   - Multiple timers
   - Old unused screens
4. Produce a FILE DECISION TABLE:
   File name | Current role | Required role | Action (Keep / Refactor / Delete)

RULES:
- Do NOT change code.
- Do NOT rename files.
- Do NOT add new files.
✅ Phase success when:
Every file has one clear fate.
________________________________________
🧠 PHASE 3 — CORE LOGIC REBUILD (NO UI)
🎯 PURPOSE
Fix the real problem: logic is scattered.
📌 PHASE 3 PROMPT
PHASE 3 — CORE LOGIC REALIGNMENT

Goal: Ensure ONE authority per responsibility.

Implement or refactor ONLY these core classes:

1. ModeManager
   - Handles mode activation, priority, schedule
   - NO UI, NO overlays

2. RuleManager
   - Decides allow / alert / block
   - Input: mode + rules + source
   - Output: enforcement decision ONLY

3. EnforcementExecutor
   - Executes block, exit, countdown
   - NEVER decides whether to block

4. OverlayController
   - Decides WHICH overlay to show
   - OverlayService becomes UI-only

DELETE:
- Any class directly blocking apps outside EnforcementExecutor
- Any timer inside Activity / Fragment

RULES:
- NO UI CHANGES
- NO NEW FEATURES
- LOGIC ONLY
✅ Phase success when:
No UI or service decides enforcement anymore.
________________________________________
🪟 PHASE 4 — OVERLAY SYSTEM REWRITE (STRICT)
🎯 PURPOSE
Overlays are enforcement, not decoration.
📌 PHASE 4 PROMPT
PHASE 4 — OVERLAY SYSTEM

1. Refactor OverlayService:
   - Only inflates layouts
   - Forwards button clicks
   - NO decision logic
   - NO countdown logic

2. OverlayController decides:
   - Gentle / Reminder / Strict / Enforcement
   - Priority replacement
   - Mode-based styling

3. Implement correct exit behavior:
   - Apps → exit app
   - Sites → close tab
   - Shorts/Reels → exit feed ONLY

RULES:
- Do NOT redesign overlay UI yet.
- Behavior correctness > appearance.
✅ Phase success when:
Overlay cannot appear without OverlayController.
________________________________________
🎨 PHASE 5 — UI REDESIGN (VISIBLE CHANGE STARTS HERE)
⚠️ This is where your previous attempt FAILED
because this phase was never explicit.
________________________________________
📊 PHASE 5A — DASHBOARD
PHASE 5A — DASHBOARD UI REDESIGN

Redesign Dashboard screen to EXACTLY include:

- Focus Score (top)
- Current Active Mode (text)
- Monitoring toggles (Apps / Sites / Shorts)
- AI Insights section (static placeholder)
- Bottom navigation bar

RULES:
- Dashboard reads data only
- ZERO enforcement logic
________________________________________
📱 PHASE 5B — MONITORING SCREENS
PHASE 5B — MONITORING UI

Implement 3 screens:
1. App Monitoring
2. Website Monitoring
3. Shorts/Reels Monitoring

Each item must show:
- Name + icon
- Enabled toggle
- Alert stages enabled
- Click → configuration screen

RULE:
UI edits rules only, never enforces.
________________________________________
⚙️ PHASE 6 — CONFIGURATION MODULES
PHASE 6 — CONFIGURE ALERTS

For App / Site / Shorts / Reels:

Screen must include:
- Threshold spinners (reuse existing logic)
- Custom message fields
- Strict block duration
- Manual override toggle
- Save button

RULE:
Save → writes to RuleStore only.
________________________________________
📊 PHASE 7 — ANALYTICS UI
PHASE 7 — ANALYTICS UI

Implement:
- Date selector
- Usage Time tab
- App Launches tab
- Timeline tab

RULE:
Analytics screen is READ-ONLY.
________________________________________
🧠 PHASE 8 — MODES UI
PHASE 8 — MODES UI

Screen must show:
- Focus
- Work
- Study
- Sleep
- Custom

Activate mode → request ModeManager.
________________________________________
🧹 PHASE 9 — DELETE & CLEAN
PHASE 9 — CLEANUP

Delete:
- Any file marked DELETE in Phase 2
- Duplicate services
- Old layouts not referenced
- Unused fragments

Confirm:
- No dead code
- No duplicate logic
________________________________________
✅ PHASE 10 — FINAL VERIFICATION
PHASE 10 — FINAL VERIFICATION

Confirm:
1. UI looks visibly different
2. Logic matches document
3. No abnormal behavior
4. Enforcement is centralized
5. Old wrong structure is gone
________________________________________
🔑 WHY THIS WILL WORK (IMPORTANT)
Your earlier prompts failed because:
•	They assumed agent “already understands”
•	They mixed analysis + action
•	They didn’t force document-first thinking
This version:
•	Forces understanding
•	Forces audit
•	Forces deletion
•	Forces visible UI redesign
________________________________________
🚀 NEXT STEP (VERY IMPORTANT)
Do NOT send all phases at once.
Send ONLY:
👉 MASTER RESET PROMPT
Then Phase 1, wait for completion, then Phase 2…
If you want, next I can give you:
•	✅ One-by-one copy-paste prompts (clean format)
•	✅ A checklist to verify each phase output
•	✅ A “what to do if agent deviates” recovery prompt
Just tell me which one.

